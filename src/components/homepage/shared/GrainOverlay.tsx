import React from 'react'

/** Fixed ambient film-grain texture over the whole page — subtle, never interactive. */
export const GrainOverlay: React.FC = () => <div aria-hidden="true" className="grain-overlay" />
